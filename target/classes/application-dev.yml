camel:
  springboot:
    # The Camel context name
    name: CamelRestSql
  component:
    servlet:
      mapping:
        enabled: true
        context-path: /api/Mobile/*

# Binding health checks to a different port
management:
  port: 8081

# disable all management enpoints except health
endpoints:
  enabled: false
  health:
    enabled: true

spring:
  # Spring JDBC configuration
  datasource:
    url:      jdbc:sqlserver://mobadserver.database.windows.net;databaseName=Mobad
    username: Mobadmin
    password: P@ssw0rd
    driverClassName : com.microsoft.sqlserver.jdbc.SQLServerDriver
    
#    spring:
  # Spring JDBC configuration
 # datasource:
  #  url:      jdbc:mysql://${${mysql.service.name}.service.host}:${${mysql.service.name}.service.port}/${mysql.service.database}
  #  username: user7SS
  #  password: qLipb2eRpLsKdJLT
    

  # Spring JDBC configuration
test:
  datasource:
   url: jdbc:oracle:thin:@172.20.10.2:1521/orcl
   username: super_conf
   password: super_conf

# The application configuration properties
quickstart:
  generateOrderPeriod: 2s
  processOrderPeriod: 5s
